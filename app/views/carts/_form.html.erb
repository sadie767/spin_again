<div class="edit_cart">
  <%= form_for item do |f| %>
    <%= f.hidden_field :product_id, value: item.product_id %>
    <%= f.number_field :quantity %>
    <%= f.submit "Update cart" %>
    <% end %>
</div>
